void knightMovement(struct Tile *pTile, struct Piece *pKnight) {

	int knightPosition;
	knightPosition = pKnight->position;
	int forwardMovement = 16;
	int backwardMovement = 16;
	int rightMovement = 2;
	int leftMovement = 2;

	if (rightMovement >= 8) {

	} 


	if (leftMovement >= 8) {

	} 
}
